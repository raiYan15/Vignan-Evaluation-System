{
  "name": "vignan-evaluator-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "server:dev": ".\\.venv\\Scripts\\python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload",
    "server:start": ".\\.venv\\Scripts\\python -m uvicorn app.main:app --host 127.0.0.1 --port 8001",
    "server:start:8000": ".\\.venv\\Scripts\\python -m uvicorn app.main:app --host 127.0.0.1 --port 8000",
    "test": ".\\.venv\\Scripts\\python -m pytest -q"
  }
}
